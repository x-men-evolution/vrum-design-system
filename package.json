{
  "name": "vrum-design-system",
  "private": true,
  "packageManager": "npm@11.4.2",
  "engines": {
    "node": ">=24"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "tokens:build": "turbo run build --filter=@x-men-evolution/design-tokens"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
